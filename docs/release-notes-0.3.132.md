## DFlash Console 0.3.132

### Fix — DFlash stack registration and loading

- Hide incomplete or missing-file DFlash profiles from the Playground.
- Keep the OpenAI-compatible model list limited to complete, unique entries.
- Preserve distinct registered DFlash profiles when catalog files overlap.
- Add regression coverage for gateway filtering, deduplication, and catalog
  profile selection.

### Install

- **Windows:** `DFlash-Console-Setup-0.3.132-x64.exe` from GitHub Releases
- **CLI:** `pip install dflash-console==0.3.132`
